version: "2"

linters:
  default: standard
  enable:
    - gosec
  settings:
    gosec:
      excludes:
        - G104
        - G115
        - G404

formatters:
  enable:
    - gofmt
    - goimports
